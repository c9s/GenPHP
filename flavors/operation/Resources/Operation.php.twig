<?php 
namespace GenPHP\Operation;
use GenPHP\Operation\Helper;

class {{className}} extends Operation
{

    function run( /* arguments */ )
    {
        // $this->logAction('copy',$to);
        // $rsDir = $this->getResourceDir();

        // do something 
    }
}

